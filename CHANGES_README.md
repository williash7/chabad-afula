# שדרוגים שבוצעו — קמפיין הוראות קבע

## א' + ב'. רשימת הוראות קבע מסודרת + זיהוי ישנות שמסתיימות

- **קובץ חדש:** `src/lib/standingOrders.ts` — קובע לכל הוראת קבע סטטוס: `active` (פעילה) /
  `expiring` (מסתיימת בקרוב) / `expired` (הסתיימה), לפי שדה ה-`remaining` (כמה חיובים נותרו)
  שכבר מוחזר מה-API הקיים (`getHK`).
- **מסך חדש:** `src/components/StandingOrdersModal.tsx` — רשימה מלאה, ממוינת כך שמה שדחוף
  (כשל חיוב, ואז מסתיימות בקרוב) עולה למעלה. אפשר לחפש, לסנן, ולשנות את סף ה"מסתיימת בקרוב"
  (כברירת מחדל: 2 חיובים או פחות שנותרו).
- נפתח בלחיצה על הכרטיס "הוראות קבע" במסך **דוחות**.

## ג'. תזכורת חודשית מסונכרנת עם כשלי חיוב

- באנר בדשבורד (מסך הבית) שמופיע **פעם בכל חודש קלנדרי** אם יש הוראות קבע שמסתיימות/הסתיימו
  או עם כשל חיוב פתוח. יש כפתור "סמן כנבדק החודש" וכפתור לצפייה ברשימה המלאה.

**מגבלה חשובה שכדאי לדעת:** האפליקציה היא אתר סטטי לגמרי בלי שרת משלה (כפי שכתוב ב-README
המקורי) — לכן זו **תזכורת שמופיעה כשפותחים את האפליקציה**, ולא התראת Push אמיתית שתגיע
גם כשהטלפון בכיס והאפליקציה סגורה.

אם תרצה התראה אמיתית כל חודש (למשל מייל שמגיע אוטומטית ב-1 לחודש, בלי צורך לפתוח את
האפליקציה בכלל) — זה אפשרי, אבל דורש הוספת טריגר מבוסס-זמן ל-Google Apps Script שמריץ את
ה-backend (הסקריפט הזה לא נכלל בקובץ שהעלית, כך שלא יכולתי לגעת בו ישירות). דוגמת קוד
שאפשר להדביק שם:

```javascript
function sendMonthlyHkReminder() {
  var hk = getHK().hk; // משתמש בפונקציה הקיימת שלך שמחזירה את רשימת ההוק
  var failures = getFailures().failures;
  var expiring = hk.filter(function(h) { return h.active && h.remaining <= 2; });
  var expired = hk.filter(function(h) { return !h.active || h.remaining <= 0; });

  var body = 'מסתיימות בקרוב: ' + expiring.length + '\n' +
             'הסתיימו: ' + expired.length + '\n' +
             'כשלי חיוב: ' + failures.length;

  MailApp.sendEmail('YOUR_EMAIL@example.com', 'בדיקה חודשית — הוראות קבע', body);
}
```
ואז ב-Apps Script: **Triggers → Add Trigger → Time-driven → Month timer → 1st of month**.
(יש להתאים את שמות הפונקציות/שדות למבנה המדויק של הסקריפט שלך.)

## ד'. מכתב תודה מעוצב

- **קובץ חדש:** `src/components/ThankYouLetterModal.tsx` — תעודת הוקרה מעוצבת (לא רק הודעת
  טקסט כמו שהיה קודם) עם שם התורם, הסכום והתאריך, שמתמלאים אוטומטית מהתרומה הספציפית.
- כפתור **"שליחה ב-WhatsApp"**: במובייל פותח את חלונית השיתוף המערכתית עם התמונה מצורפת
  (Web Share API); בדפדפן שלא תומך בזה, מוריד את התמונה ופותח שיחת WhatsApp עם טקסט מוכן.
- כפתור **"שליחה במייל"**: מוריד את התמונה ופותח את תוכנת המייל עם נושא וגוף מוכנים
  (יש לצרף את התמונה שהורדה — לינק `mailto` בדפדפן לא יכול לצרף קבצים אוטומטית).
- כפתור **"הורדה"** להורדת התמונה בלבד.
- נגיש משני מקומות: ליד כל תרומה ב"תרומות אחרונות" בדשבורד, וב"היסטוריה מלאה" בפרופיל
  כל תורם.

## הערה טכנית

לא הייתה לי גישה לאינטרנט בסביבת העבודה כדי להריץ `npm install` + `npm run lint` באופן מלא
מול הפרויקט, ולכן לא הרצתי בדיקת קומפילציה חיה. עברתי ידנית על כל הקוד (איזון סוגריים,
טיפוסים, ייבואים) ותיקנתי בעיה אחת שמצאתי (מערך עם צורת אובייקטים לא אחידה ב-ReportsTab).
מומלץ בכל זאת להריץ `npm run build` (או `npm run lint`) פעם אחת אצלך לפני שמעלים לפרודקשן,
ליתר ביטחון.
